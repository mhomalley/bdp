.menu_title
  Image: 
  = @image.id
Issue: 
%br/
= link_to(d(@issue.date), new_admin_issue_article_path(:issue_id => d(@issue.date)))
%br/
%br/
= link_to 'Remove the Cache for this Issue', kill_issue_cache_path(:id => d(@issue.date)) unless @issue.new_record?
%br/
%br/
%br/
= link_to('delete', admin_issue_image_path(:id => @image.id, :issue_id => d(@image.issue.date)), :method => :delete, :confirm => 'Are you sure you want to delete this image? This action cannot be undone.') unless @image.new_record?
